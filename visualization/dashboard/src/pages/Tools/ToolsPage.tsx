import React from 'react';
import { ToolCatalog } from '../../features/tools/components/catalog';

const ToolsPage: React.FC = () => {
  return (
    <div className="h-full">
      <ToolCatalog />
    </div>
  );
};

export default ToolsPage;